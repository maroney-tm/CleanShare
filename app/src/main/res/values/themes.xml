<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme (used by <application> element) -->
    <style name="Theme.AndroidShareSanitizer" parent="android:Theme.Material.Light.NoActionBar" />

    <!--
        Alias required by the legacy android:theme attribute on activities that use Compose.
        We don't have com.google.android.material as a dep, so we define the name ourselves.
        The window chrome is irrelevant — Compose owns all rendering.
        See DECISIONS.md §T2.
    -->
    <style name="Theme.Material3.DayNight.NoActionBar"
        parent="android:Theme.Material.Light.NoActionBar" />

    <!--
        ShareActivity is a transparent trampoline: no UI is shown.
        windowIsTranslucent keeps the previous app visible behind it.
        backgroundDimEnabled=false prevents the screen from darkening.
        See DECISIONS.md §Revamp.
    -->
    <style name="Theme.Transparent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>
